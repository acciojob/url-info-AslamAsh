//your JS code here. If required.
let url = window.location.href;

// Count number of letters in the URL
let length = url.length;

// Show result using alert
alert("The length of the URL is: " + length);